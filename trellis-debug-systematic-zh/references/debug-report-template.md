# 调试记录模板

一次调试一份，全程回写。它既是给小模型的"工作记忆"（防止忘了已试过什么），也是升级给强模型时的交接材料。

## 失败信号

- 复现命令：`<command>`
- 原文（堆栈/断言/退出码）：

```
<贴失败原文>
```

- 是否稳定复现：是 / 否（否则先解决不稳定复现）

## 定位过程

| 用了哪招 | 结果（缩小到哪里） |
| --- | --- |
| 读栈 / 二分注释 / 加日志 | <文件:行 / 变量实际值> |

## 假设记录

| 轮次 | 假设（具体到变量/分支/契约） | 验证方式 | 结论(成立/证伪) |
| --- | --- | --- | --- |
| 1 | <假设> | <怎么验证> | <结论> |

已排除项：

- <排除的假设，避免重复尝试>

## 修复

- 根因：<已验证的根因>
- 改动位置（一处）：<file:行>
- 重跑原失败命令结果：绿 / 仍红(已回滚)

## 防御性回归

- 这个 bug 能否从别处再发生：能 / 不能
- 若能：已在 `check.jsonl` 记回归点 `<path>`，回归测试：<test 或 "待补">

## 退出状态

- [ ] 绿了，回到 TDD 循环失败的那一步
- [ ] 已回滚，升级（附已排除项）
- [ ] 超 3 轮，升级强模型/人工
