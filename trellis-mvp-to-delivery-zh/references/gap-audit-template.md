# MVP 差距审计模板

已有 MVP 后的第一轮使用此模板。用户确认审计前，不要写代码或创建任务。

## Requirements Traceability Matrix

| ID | 需求 | 当前状态 | 相关代码 | 现有测试 | 缺口 | 建议 Task |
| --- | --- | --- | --- | --- | --- | --- |
| REQ-001 |  | UNCLEAR |  |  |  |  |

允许的状态：

- `DONE`：已完整实现且有测试。
- `PARTIAL`：部分实现。
- `MISSING`：未实现。
- `UNTESTED`：已实现但缺少足够测试。
- `UNCLEAR`：需求不够清楚，无法实现。

证据规则：

- `DONE` 必须有实现证据和测试证据。
- `UNTESTED` 必须有实现证据和明确测试缺口。
- `PARTIAL` 必须说明哪些已完成、哪些缺失。
- `MISSING` 表示没有实现证据，或只有无关脚手架。
- `UNCLEAR` 必须给出阻塞问题或歧义说明。

## MVP 完成度摘要

- 已完成：
- 部分完成：
- 未实现：
- 已实现但未测试：
- 不明确：

## 阻塞性问题

只列阻塞实现的问题。不要询问可从仓库或源需求中回答的问题。

## 任务计划

| 顺序 | Task Slug | 标题 | 需求 ID | 依赖 | 验收标准 | 自动化测试要求 | 优先级 | 复杂度 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |  | P0 | 低 |

复杂度（按执行模型能力评估，决定拆分粒度和 PRD 详细度）：

- `低`：标准 CRUD、配置、有现成范例可照抄。弱模型可独立完成。
- `中`：含少量业务校验或跨表逻辑，需要明确实现步骤。弱模型在详细 PRD 下可完成。
- `高`：复杂事务、并发、跨模块一致性、需要大量隐性领域知识。弱模型难以独立完成，应进一步拆碎，或在 PRD 里把每步定死到无需推理。

任务规划规则：

1. 一个任务只关闭一组高度相关的缺口。
2. 除非不可分离，不要把基础契约和 UI 展示混在一起。
3. 不要把功能实现和最终验证混在一起。
4. 每个功能任务都包含自己的基础测试。
5. 最终验证任务依赖所有功能任务。

## 确认请求

最后输出：

```text
请确认这个差距审计和交付任务计划。如果确认，我将创建 Trellis tasks 和 PRD，但暂不实现功能。
```
